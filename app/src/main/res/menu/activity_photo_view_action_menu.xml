<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/viewer_action_photo_open"
        android:contentDescription="@string/viewer_open"
        android:icon="@drawable/ic_open"
        android:title="@string/viewer_open"
        app:showAsAction="ifRoom"
    />
    <item
        android:id="@+id/viewer_action_photo_download"
        android:contentDescription="@string/viewer_download"
        android:icon="@drawable/ic_download"
        android:title="@string/viewer_download"
        app:showAsAction="ifRoom"
    />
</menu>
